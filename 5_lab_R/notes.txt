КРАТКИЙ ГАЙД ЧТО И КАК ДЕЛАТЬ:
На данный момент сделано всё, кроме проверки на Компоненты Сильной Связности (самая жопа)
Всё остальное видно в меню, если будут вопросы конечно же пиши
Ниже представлены команды, которые могут понадобиться --->

Компилировать с проверкой на утечки(я не нашёл способов сломать программу):
    gcc -ggdb -o main.out main.c funcs.c struct.c -lm  -fsanitize=address -fno-omit-frame-pointer

Просто запустить(Всё равно будет видно утечки памяти если есть):
    ./main.out

Запустить с дебаггером:
    lldb main.out

Нарисовать граф(после того как выбран соответствующий пункт в меню):
    xdot graph.dot